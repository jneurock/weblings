:: TODO:
::   * Check if node is installed and if the version is new enough
::     * If not is not installed, install it
::     * If node is installed but version too old, warn and exit
::     * If node is installed and new enough
::       * Check if npm is installed and if the version is new enough
::         * If npm is installed but too old, upgrade it
::   * Run npm i
